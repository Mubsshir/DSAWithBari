#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <map>
using namespace std;

// map all the data
map<string, string> TagMap;

int main()
{
    TagMap.insert() int n, q;
    cin >> n >> q;
    cin.ignore();
    cout << q;
    string line;
    while (n--)
    {
        getline(cin, line);
        printf("___");
        cout << line;
    }
    return 0;
}
